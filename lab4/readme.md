# Алгоритмы отсечения отрезков и многоугольников

Этот проект демонстрирует работу алгоритмов отсечения отрезков и многоугольников, реализованных на Java Swing. Программа предоставляет визуализацию процесса отсечения и позволяет наглядно изучать принципы работы различных алгоритмов компьютерной графики.

## Реализованные алгоритмы

### Алгоритм средней точки (Midpoint Algorithm)
- Используется для отсечения отрезков прямоугольным окном
- Основан на принципах разбиения отрезка и проверки видимости его средней точки
- Эффективно обрабатывает различные случаи расположения отрезков относительно окна

### Алгоритм отсечения выпуклого многоугольника (Sutherland-Hodgman Algorithm)
- Применяется для отсечения выпуклых многоугольников прямоугольным окном
- Последовательно обрабатывает каждую границу отсекающего окна
- Сохраняет правильную геометрию отсеченного многоугольника

## Особенности проекта

- **Интерактивный интерфейс** - ввод данных через текстовое поле с подсветкой формата
- **Визуализация в реальном времени** - отображение исходных и отсеченных объектов
- **Система координат** - масштабируемая декартова система с осями X и Y
- **Цветовая дифференциация**:
  - 🟢 Зеленый - отсекающее окно
  - 🔴 Красный - исходные отрезки
  - 🔵 Синий - отсеченные отрезки и многоугольники
- **Автоматическая валидация** - проверка корректности вводимых данных

## Формат ввода данных

```
количество_отрезков
x1₁ y1₁ x2₁ y2₁
x1₂ y1₂ x2₂ y2₂
...
x1ₙ y1ₙ x2ₙ y2ₙ
xmin ymin xmax ymax
```

### Пример 1: Базовый тест
```
5
50 50 200 200
100 100 300 300
150 50 150 250
80 180 280 80
120 120 250 250
100 100 400 400
```

**Описание**: В этом примере представлены 5 отрезков с различной ориентацией и прямоугольное окно отсечения. Алгоритм средней точки определит видимые части каждого отрезка.

### Пример 2: Множественные пересечения
```
8
50 50 200 200
100 100 300 300
150 50 150 250
80 180 280 80
120 120 250 250
350 150 450 250
50 300 150 400
300 50 400 150
100 100 400 400
```

**Описание**: Демонстрирует работу алгоритма с отрезками, имеющими разные типы пересечений с окном - полная видимость, частичная видимость и полная невидимость.

### Пример 3: Отсечение многоугольника
```
1
150 150 250 150 250 250 150 250
100 100 400 400
```

**Описание**: Тестовый случай для алгоритма Сазерленда-Ходжмана, где квадратный многоугольник отсекается по границам прямоугольного окна.

## Визуализация результатов

После выполнения отсечения на координатной плоскости отображаются:

- **🔴 Красные отрезки** - исходные геометрические объекты до отсечения
- **🟢 Зеленый прямоугольник** - границы отсекающего окна
- **🔵 Синие отрезки/многоугольники** - результирующие видимые части после применения алгоритма отсечения

## Особенности алгоритмов

### Алгоритм средней точки
- Рекурсивно делит отрезки для определения видимых частей
- Эффективно обрабатывает пограничные случаи
- Обеспечивает точное определение точек пересечения

### Алгоритм Сазерленда-Ходжмана
- Последовательно обрабатывает каждую границу отсекающего региона
- Сохраняет топологию многоугольника
- Гарантирует корректность результата для выпуклых окон отсечения

## Использование программы

1. **Загрузка данных** - введите данные в текстовое поле или используйте готовые примеры
2. **Выбор алгоритма** - укажите требуемый метод отсечения
3. **Визуализация** - наблюдайте исходные и результирующие объекты на координатной плоскости
4. **Анализ** - изучайте работу алгоритмов на различных тестовых случаях

